# Repository Instructions

- For Node.js and npm commands, load NVM first and use Node `22.17.1` unless a more specific task instruction says otherwise:
  `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 22.17.1`.
